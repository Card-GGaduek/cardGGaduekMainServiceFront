server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # 요청 경로가 파일이나 폴더가 아니면 index.html 반환 (SPA 경로 처리를 위해)
        try_files $uri $uri/ /index.html;
    }
}